# 任务1.1 Skill 1.6.0 工程基线纠偏与首次产物审计计划

## 任务目标
严格执行 `tasks/任务1.1_Skill1.6.0工程基线纠偏与首次产物审计01.md`，基于官方 Skill 1.6.0、`docs/05` V1.1、`docs/07` 与 `docs/08`，对任务1已产出的工程骨架进行全方位盘点、冲突分析、架构纠偏（建立统一命名空间 `SuShiUniverse`、完善 Native Bridge 安全骨架 `XHSBridge`、规范 Classic Script 组织）、静态合规扫描与双重官方产物审计（产物目录与阶段性 ZIP 包），确保零阻塞性错误并通过自验证。

## 执行步骤
1. **盘点任务1实际工程与建立冲突矩阵**：
   - 梳理现有文件树与代码实现；
   - 对照 Skill 1.6.0 硬约束输出冲突与对齐矩阵，识别待纠偏项（例如：原生脚本组织从单文件升级为 `namespace.js` + `bridge.js` + `app.js`，Native Bridge 封装骨架等）。
2. **工程架构与代码纠偏**：
   - 建立经典脚本命名空间：`js/namespace.js`（`window.SuShiUniverse = window.SuShiUniverse || {};`）；
   - 建立平台桥接适配层骨架：`js/bridge.js`（提供 `isAvailable`, `writeTempFile`, `saveImage`, `postNote`, `openRedPage` 的能力检测与安全封装）；
   - 规范应用主逻辑：`js/app.js`（集成 Slogan 交互与 Bridge 探测）；
   - 规范样式与布局：`css/style.css`（符合 Chrome 61 兼容层与双兼容安全区）；
   - 更新入口 `index.html`：按依赖顺序外置引入经典脚本。
3. **静态合规性扫描与构建流程更新**：
   - 升级 `scripts/build_and_audit.py`：支持根据最新规范将标准产物（`index.html`、`css/`、`js/`、`assets/`、`data/`）输出至 `dist/`，打包生成 `dist/su-shi-universe-xhs.zip`；
   - 逐项扫描 CSP、禁用网络 API、外部链接及文件扩展名白名单。
4. **执行官方产物双重审计**：
   - 运行官方 Python 审计脚本检查 `dist/` 目录与 `dist/su-shi-universe-xhs.zip`；
   - 运行官方 Node.js 审计脚本验证；
   - 验证 ZIP 解压后根目录直接包含 `index.html`，确保包体积控制在 2 MiB 推荐目标内（实测约几 KB）。
5. **本地运行验证与验收归档**：
   - 启动本地服务进行功能验证；
   - 编写 `tasks/results/20260907_任务1.1_Skill1.6.0工程基线纠偏与首次产物审计01成果说明.md` 与 `Walkthroughes/20260907_任务1.1_Skill1.6.0工程基线纠偏与首次产物审计成果说明.md`；
   - 追加记录至 `开发日记.md`；
   - 提交 Git Commit。
