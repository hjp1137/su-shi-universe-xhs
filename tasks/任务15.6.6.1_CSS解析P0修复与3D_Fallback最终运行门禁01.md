# 任务15.6.6.1：CSS解析P0修复与3D/Fallback最终运行门禁

> 任务类型：15.6.6 P0专项返修 / 运行时最终门禁  
> 基线提交：`56a9f81020371a1e0ec84301f1b8b193c559bbe8`  
> 优先级：**P0**  
> 前置状态：任务15.6.6工程实现完成，但直接运行生产ZIP复验发现CSS解析级P0，**任务15.6.6暂不得签PASS，不得进入任务16**。

---

## 一、任务目标

本任务不再进行任何泛化视觉优化，只关闭任务15.6.6直接运行审核中发现的**1个确定P0**，并把此前自动测试遗漏的“CSS源码存在但浏览器未真实解析”问题补成长期运行门禁。

本任务完成后必须同时证明：

1. `css/components.css` 语法结构完整，浏览器能够解析到文件末尾；
2. 15.6.6新增的2.5D Fallback相关选择器真实进入浏览器CSSOM；
3. WebGL关闭时，诗词星群实际呈现为**单层、纯净、可读的2.5D东方星宿环**；
4. WebGL正常时，仍保持**单层PoetryPlanet**，不得重新出现旧二维星图叠层；
5. WebGL模式实际拖拽时，移动的是诗词星球主体而非独立光团；
6. 点击作品星后，目标星实际完成自动旋转、前置、放大与附着式信息展示；
7. 全量历史测试、官方Skill审计、最终生产ZIP验证重新全部通过；
8. 最终成果必须commit并push到`origin/main`，再生成新的最终生产ZIP交由项目组直接运行复验。

---

## 二、已确认P0根因

直接运行任务15.6.6最终生产ZIP后发现：

`css/components.css` 中存在未闭合规则：

```css
.constellation-nebula-glow {
  position: absolute;
  top: 0;

/* 任务 15.6.6: 单层纯净 2.5D 东方星宿降级环 */
.poetry-constellation-25d-fallback {
```

即 `.constellation-nebula-glow` 缺失闭合 `}`。

已确认后果：

- `components.css` 左右花括号数量不平衡；
- 浏览器CSSOM无法正常解析后续一批15.6.6新增规则；
- `.poetry-constellation-25d-fallback`、`.fallback-focus-node`、`.fallback-orbit-star`、`.fallback-orbit-title` 等Fallback关键样式未真实生效；
- WebGL关闭后的实际页面出现裸文本、白色默认控件、标签堆叠等明显成品级失败；
- 现有自动测试仅检查源码字符串存在，没有检查浏览器是否真正解析，因此错误被漏检。

**禁止仅修改测试以“适配现状”。必须先修实际CSS语法与运行效果。**

---

# 三、P0-A：修复 `components.css` 解析结构

## 3.1 必须修复缺失闭合

优先检查 `.constellation-nebula-glow` 及其相邻规则，正确恢复完整CSS结构。

不得只机械补一个 `}` 后结束，必须对整个：

- `css/components.css`
- `css/views.css`
- `css/tokens.css`
- 其他生产CSS

进行完整结构校验，确认无同类未闭合问题。

## 3.2 新增静态CSS语法门禁

新增专项测试，建议：

`scripts/test_css_runtime_parse_15_6_6_1.py`

至少校验：

- `{` / `}` 结构平衡；
- 注释块完整；
- 文件末尾规则未被意外吞并；
- 关键选择器仅出现于合法规则位置。

但注意：

> **花括号计数只能作为第一层检查，不得把它当成唯一验收。**

---

# 四、P0-B：增加浏览器CSSOM真实解析门禁

必须新增浏览器运行级测试，不能再只 grep / 查字符串。

启动真实浏览器加载生产页面后，读取 `document.styleSheets[].cssRules` 或等价CSSOM接口，明确断言以下选择器可被浏览器真实解析：

```text
.poetry-constellation-25d-fallback
.constellation-fallback-wrap
.fallback-focus-node
.fallback-orbit-star
.fallback-orbit-title
.constellation-catalog-panel
.poetry-planet-attached-capsule
.constellation-3d-hint
```

至少要求：

1. 选择器真实存在于CSSOM；
2. 对应计算样式不是浏览器默认裸样式；
3. 规则顺序没有因前一条非法CSS被吞掉；
4. 生产`dist/`和最终ZIP内的CSS都必须接受同样检查。

建议将CSSOM测试纳入：

- `scripts/build_and_audit.py`
- 或新增独立P0脚本并由build/audit调用。

此门禁必须永久保留，防止后续再次出现“源码写了规则但浏览器根本没解析”的假PASS。

---

# 五、P0-C：2.5D Fallback真实运行复验

## 5.1 必须强制关闭WebGL后实际打开诗词星群

不得仅调用Fallback函数或截图静态DOM。

浏览器测试必须模拟/强制：

```text
WebGL unavailable
```

并走真实用户路径进入至少一个站点诗词星群，例如：

```text
首页 → 人生星河 → 黄州 → 诗词星群
```

## 5.2 Fallback视觉硬要求

实际页面必须满足：

- 只存在**一套**2.5D星群；
- 不存在WebGL Canvas与2D旧星图同时显示；
- 不存在白色浏览器默认Chip / button；
- 不存在大量作品标题互相压叠；
- 星体呈环形/星宿式空间分布；
- 中央精神星核明确；
- 作品标题只显示必要数量，远端/次要标签应弱化或隐藏；
- 点击目标作品后可以自然聚焦或高亮；
- 仍可进入作品赏析；
- 视觉风格与主站深靛、青绿、古金体系一致。

## 5.3 Fallback不得以“功能能点”为PASS标准

下列情况一律FAIL：

- 作品全部变成纵向普通列表；
- 浏览器默认白按钮；
- 白底Chip堆叠；
- 旧二维星球 + 新Fallback重复两层；
- 文本超出容器；
- 作品名大面积重叠；
- 用户无法辨别星群主体。

---

# 六、P0-D：WebGL主模式最终动态运行门禁

本任务虽然根因是CSS，但由于15.6.6直接审核环境无法稳定完成全套软件WebGL长时间动态复验，因此本任务必须由开发环境补齐真实动态验收证据。

## 6.1 单层架构硬断言

WebGL可用时，实际DOM中必须满足：

```text
.poetry-constellation-3d-stage = 1
canvas(WebGL) = 1
.constellation-star-node = 0
.star-node-img = 0
旧SVG orbit = 0
旧coreNode = 0
旧独立bubble/card = 0
```

不得仅凭源码`if/else`判断，必须在浏览器运行时检查。

## 6.2 星球主体拖拽坐标门禁

选择同一颗作品星，记录拖拽前后该`PoetryPlanet`主体的屏幕投影坐标：

```text
before: x1, y1
拖拽整个星群
 after: x2, y2
```

必须证明：

```text
abs(x2-x1) + abs(y2-y1) > 合理阈值
```

并且被测对象必须是**星球主体/PoetryPlanet**，不得拿halo、辅助光团、背景粒子代替。

## 6.3 点击聚焦硬门禁

点击任一作品星（建议《念奴娇·赤壁怀古》）后必须真实观察到：

1. 星群自动旋转；
2. 目标星进入中央/中央偏上主视区；
3. 目标星放大约1.8～2.3倍；
4. 其他星体退后、弱化或缩小；
5. 作品信息贴附星球附近；
6. 不出现独立大矩形信息卡；
7. “进入作品赏析”入口保持可用。

必须提供至少：

- 聚焦前截图；
- 聚焦中/后截图；
- 或短录屏/连续帧证据。

---

# 七、允许顺手关闭的P1：站点现代共鸣残留块

直接运行15.6.6后，站点长卷中的：

`.parchment-body`

仍存在明显的深色 `rgba(...)` 承托块。

本任务允许在不扩大范围的前提下顺手关闭该P1：

- 去除剩余大面积传统承托面板；
- 保持现代共鸣/今日行动可读性；
- 采用文字色、局部细线、局部自然阴影或画面安全区建立层级；
- 禁止重新引入整块blur / backdrop-filter。

**本项为P1，不得影响P0修复节奏。**

---

# 八、明确冻结范围

本任务禁止顺手重构以下已稳定模块：

- 首页；
- 人生星河主页面；
- 东坡人生实验主体玩法；
- autoTravel评分逻辑；
- 节点弹层viewport；
- Canvas三类卡内容体系；
- 作品数据与九站作品数量；
- Router / Store主链路。

只允许修：

> CSS解析P0 + 2.5D Fallback + 3D运行门禁 + 极小范围站点残留块。

---

# 九、专项测试要求

至少新增或升级以下测试。

## 9.1 CSS静态结构

建议：

`scripts/test_css_runtime_parse_15_6_6_1.py`

断言全部生产CSS结构完整。

## 9.2 CSSOM浏览器运行测试

建议：

`scripts/test_cssom_runtime_15_6_6_1.py`

真实浏览器加载页面并确认关键selector进入CSSOM。

## 9.3 Fallback运行测试

建议：

`scripts/test_constellation_fallback_runtime_15_6_6_1.py`

强制WebGL关闭后检查真实computed style、布局和交互。

## 9.4 WebGL主模式动态测试

建议：

`scripts/test_constellation_webgl_runtime_15_6_6_1.py`

必须覆盖：

- 单层DOM；
- PoetryPlanet拖拽位移；
- 目标星聚焦；
- 无旧2D层；
- 无独立大弹框。

---

# 十、必须重新执行的回归与审计

P0关闭后必须重新执行：

```text
python scripts/test_visual_p0_clear_15_6_6.py
python scripts/test_css_runtime_parse_15_6_6_1.py
python scripts/test_cssom_runtime_15_6_6_1.py
python scripts/test_constellation_fallback_runtime_15_6_6_1.py
python scripts/test_constellation_webgl_runtime_15_6_6_1.py
python scripts/test_poetry_constellation_3d_15_6_4.py
python scripts/test_visual_polish_15_6_5.py
```

然后执行：

```text
全量 scripts/test_*.py
python scripts/build_and_audit.py
python scripts/verify_final_package.py
```

所有测试必须基于**最终将被打入ZIP的生产代码**运行。

---

# 十一、人工截图/运行证据

成果中至少提供：

1. WebGL模式星群初始态；
2. WebGL拖拽后同一星球明显发生位置变化；
3. WebGL目标星聚焦态；
4. WebGL模式DOM检查证据（无旧2D节点）；
5. WebGL关闭后的2.5D Fallback完整态；
6. Fallback点击作品后的聚焦/进入态；
7. CSSOM关键selector实际解析结果；
8. 390×844至少一张完整站点截图；
9. 若顺手关闭P1，提供现代共鸣段落新截图。

截图目录建议：

`Walkthroughes/screenshots/20260908_任务15.6.6.1_CSS解析P0修复与3D_Fallback最终运行门禁/`

---

# 十二、成果说明文件

任务完成后必须生成：

`tasks/results/20260908_任务15.6.6.1_CSS解析P0修复与3D_Fallback最终运行门禁01成果说明.md`

成果说明必须如实记录：

- 缺失`}`的准确位置与根因；
- 修复方式；
- CSSOM门禁实现方式；
- Fallback真实运行结果；
- WebGL动态运行结果；
- 测试与审计结果；
- 最终ZIP大小；
- 最终commit SHA；
- 是否仍存在P0/P1/P2。

禁止用“源码已有选择器”“字符串存在”代替浏览器运行证据。

---

# 十三、Git提交与推送硬约束

继续继承项目统一要求：

> **任何任务只有在代码、测试、成果说明、截图、最终生产ZIP全部完成，并正式commit + push到远程Git仓库后，才允许报告“任务已完成”。**

本任务必须：

1. 基于当前`main`最新状态执行；
2. 所有修改完成后提交；
3. `git push origin main`；
4. 确认远程`main HEAD`就是本任务最终提交；
5. 工作区clean；
6. 从该最终HEAD重新生成生产ZIP；
7. 将最终ZIP交由项目组直接运行复验。

禁止：

- 只commit不push；
- push后继续修改但不重新打包；
- ZIP与远程HEAD不一致；
- 本任务完成后自动继续任务16。

---

# 十四、最终PASS门槛

只有同时满足以下条件，任务15.6.6.1才能判定PASS：

- [ ] `components.css`及全部生产CSS结构合法；
- [ ] 浏览器CSSOM可真实解析到Fallback关键selector；
- [ ] WebGL关闭后Fallback实际视觉纯净可用；
- [ ] Fallback无白Chip、无多层星图、无标签严重重叠；
- [ ] WebGL模式无旧2D星图DOM；
- [ ] 拖拽时PoetryPlanet星球主体真实发生屏幕位移；
- [ ] 点击作品星可自动旋转、居中、放大；
- [ ] 选中态无独立大矩形说明卡；
- [ ] 全量历史测试全部PASS；
- [ ] Skill 1.6.0双审计PASS；
- [ ] 最终ZIP验证PASS且≤10MiB；
- [ ] 最终代码已commit并push到`origin/main`；
- [ ] 生产ZIP与远程最终HEAD一致；
- [ ] 项目组直接运行复验无新P0。

若任一P0门禁未满足：

> **任务15.6.6.1 = FAIL，不得进入任务16。**

---

## 十五、后续流程

本任务完成后**停止自动推进**。

正确流程：

```text
任务15.6.6.1实施
    ↓
commit + push origin/main
    ↓
重新生成最终生产ZIP
    ↓
项目组/ChatGPT直接运行ZIP复验
    ↓
无P0
    ↓
重新执行任务16视觉2.0与WebGL集中验收
    ↓
任务17真机终审与比赛最终交付
```
